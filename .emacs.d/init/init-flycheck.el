;; (use-package flycheck
;;   :init
;;   (add-hook 'ruby-mode-hook 'flycheck-mode)
;;   :config
;;   '(lambda ()
;;      (setq flycheck-checker 'ruby-rubocop)
;;                   (flycheck-mode 1))
;;   )
